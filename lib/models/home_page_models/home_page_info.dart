/// There is not much here this is just a model for the Home Page, we just created a constructor for the title, description and image asset.
class HomePageInfo {
  final title;
  final desc;
  final imgAsset;

  HomePageInfo(this.title, this.desc, this.imgAsset);
}
