import 'package:flutter/cupertino.dart';

Widget homePageWidget(
    {required pref,
    required title,
    required subtitle,
    required imgAsset,
    required cardColor,
    required btnColor,
    required}) {
  return Container();
}
