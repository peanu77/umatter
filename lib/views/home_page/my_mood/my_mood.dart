import 'package:flutter/material.dart';

class MyMoodPage extends StatelessWidget {
  const MyMoodPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('My Mood Page'),
    );
  }
}
